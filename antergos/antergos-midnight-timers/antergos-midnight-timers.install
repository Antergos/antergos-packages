post_install() {
    echo ">>> Systemd local configuration changed."
    echo "    Please either invoke `sudo systemctl daemon-reload` or reboot, for changes to take effect."
}

post_upgrade() {
    echo ">>> Systemd local configuration changed."
    echo "    Please either invoke `sudo systemctl daemon-reload` or reboot, for changes to take effect."
}
