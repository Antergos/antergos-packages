# Maintainer: Antergos Devs <devs@antergos.com>

pkgname=faenzaflattr-icon-theme-gtk
pkgver=0.6
pkgrel=1
pkgdesc="Flattr icon theme with Faenza (GTK) as fallback."
url="http://kde-look.org/content/show.php/FaenzaFlattr?content=164770"
license=('CCPL')
arch=('any')
depends=('faenza-icon-theme')
provides=('faenzaflatter-icon-theme')
conflicts=('faenzaflatter-icon-theme-kde')
source=('https://drive.google.com/uc?export=download&id=0BwlW9hKxf1tUYlNOajVFdEtnN1k')
md5sums=('e9ce8a6c6fbdb22ce67466e8b9c62e18')
options=(!strip)

package() {
  install -d $pkgdir/usr/share/icons
  rm -R $srcdir/FaenzaFlattr/.git
  cp -r $srcdir/FaenzaFlattr $pkgdir/usr/share/icons/

  cd ${pkgdir}/usr/share/icons/FaenzaFlattr
  sed -i 's|Inherits=|Inherits=Faenza,hicolor|g' index.theme

  find $pkgdir/usr -type f -exec chmod 644 {} \;
  find $pkgdir/usr -type d -exec chmod 755 {} \;
  find $pkgdir/usr -type f -name '.directory' -delete
  find $pkgdir/usr -type f -name '.gitignore' -delete

}
